# EasyTGWhiteListMC

Telegram-бот для управления whitelist Minecraft сервера через RCON.

Подходит для:
- Локального запуска
- Запуска на хостинге
- Белый IP не требуется

Бот работает с любым сервером, где есть поддержка RCON.

---

# 🚀 Возможности

- Подача заявок на whitelist
- Автоматическая проверка шаблона заявки
- Фильтрация ника (A-Z, a-z, 0-9, _)
- Разделение лицензии и пиратки
- Привязка Telegram ID к нику
- Локальная база игроков
- Управление whitelist через RCON
- Админ-система
- Отправка сборки (.mrpack)
- Выполнение серверных команд через Telegram
- Система жалоб (v1.0.4+)

---

# ⚙️ ОБЯЗАТЕЛЬНАЯ НАСТРОЙКА

## 1️⃣ Главный администратор

```js
const ADMIN_FILE = path.join(BOT_FOLDER, 'admins.json');
const MAIN_ADMIN = 'Errnick'; // заменить на свой username
let admins = [];
```

Замените `'Errnick'` на свой Telegram username.

---

## 2️⃣ Токены ботов

```js
const TEST_MODE = 1; // 0 — основной бот, 1 — тестовый бот

const TOKENS = {
  main: 'YOUR_MAIN_BOT_TOKEN',
  test: 'YOUR_TEST_BOT_TOKEN'
};
```

Вставьте свои токены.

---

## 3️⃣ Настройка RCON

```js
const rconConfig = {
  host: 'your_rcon_host',
  port: 12345,
  password: 'YOUR_RCON_PASSWORD'
};
```

Укажите:
- IP сервера
- Порт RCON
- Пароль

---

# 📜 Полный список команд

## 👤 Пользовательские команды (без !EC)

| Команда | Что делает |
|----------|------------|
| `заявка` | Создать заявку на whitelist |
| `жалоба` | Создать жалобу на игрока (С Версии 1.0.4) |
| `список` | Показать список игроков (локальная база) |
| `проверить <ник>` | Проверить ник в базе и на сервере |
| `админы` | Показать список админов |
| `сайт` | Отправить ссылку на магазин / донат |
| `мой ник` | Показать свой ник из базы |
| `ник` (ответом на сообщение) | Показать ник пользователя |

---

## 🔐 Админ-команды (требуют !EC или !ЕС)

| Команда | Описание |
|----------|----------|
| `админ add @user` | Добавить администратора |
| `админ remove @user` | Удалить администратора |
| `сборка` | Отправить .mrpack файл |
| `команда` | Выполнить команды через RCON |
| `добавить` | Добавить игрока без заявки |
| `удалить` | Удалить игрока из базы и whitelist |

---

# 📝 Система заявок

Заявка должна содержать **ровно 7 строк**:

```
Заявка
Ник в Minecraft
Лицензия / пиратка
Возраст
Откуда узнали о сервере
Чем будете заниматься на сервере
Почему выбрали наш сервер
```

## 🔎 Проверки

- Ник: только `A-Z a-z 0-9 _`
- Строго 7 строк
- Если шаблон нарушен — заявка отклоняется

---

## 📌 Пример корректной заявки

```
Заявка
Errnick_
Лицензия
16
Discord
Играть и помогать новичкам
Выбрал сервер за дружелюбную атмосферу
```

После отправки:
- Бот отвечает на сообщение игрока
- Появляется заявка
- Только администратор может принять или отклонить её

---

# 📝 Система жалоб (1.0.4+)

Теперь игроки могут отправлять жалобы на других игроков.  

### Как написать жалобу

```
Жалоба
<ник игрока>
<причина>
```

**Пример:**

```
Жалоба
Errnick_
Гриф
```

### Как работает

1. Игрок отправляет жалобу — создаётся карточка.
2. Админ может **рассмотреть** жалобу.
3. После рассмотрения:
   - Автор жалобы видит, что она рассмотрена ⚡
   - Автор может:
     - **Закрыть** жалобу ✅
     - **Возобновить** жалобу ↩️ (ещё не решена)
4. Возобновлённая жалоба снова активна — админ может пересмотреть.

### Оформление карточки
```
📄 Жалоба
От: <кликабельный ник автора>
На: <кликабельный ник цели>
Причина: <текст жалобы>
❗ Статус: 🔴 Не решена / ⚡ Рассмотрена / ещё не закрыта автором
```


### Кнопки управления

- `Рассмотреть ⚡` — для админа
- `Закрыть ✅` / `Оставить открытой ↩️` — для автора жалобы

> Только игрок, который создал жалобу, может закрыть или возобновить её.  
> Только админ может пометить жалобу как рассмотренную.

# 📂 Команда "Список"

Показывает игроков из локальной базы бота.

Особенности:
- Ник привязан к Telegram ID
- Ники кликабельные
- Есть ссылка на профиль Telegram

---

# 🔍 Команда "Проверить <ник>"

Проверяет:
- Есть ли игрок в базе
- Привязан ли к Telegram
- Есть ли в whitelist на сервере

Возможные статусы:
- Есть в базе и на сервере
- Только в базе
- Только на сервере
- Не найден

---

# 🌐 Команда "сайт"

В коде:

```js
if (command === 'сайт')
  return sendSafe(msg, `🌐 Наш магазин / донат: https://errnicraft.cdonate.ru/#shop`);
```

Замените ссылку на свою.

---

# 🛠 Админ-команды подробнее

## !EC админ add|remove

Пример:

```
!ЕС админ add @username
```

Даёт полный доступ к админ-командам.

---

## !EC команда

Выполняет команды через RCON.

Пример:

```
!ЕС команда
ban Errnick_ гриф
say Грифер устранён
```

Каждая команда — с новой строки.

---

## !EC добавить

Использование:

1. Ответить на сообщение игрока
2. Написать:

```
!EC добавить
UserName
Лицензия
```

Игрок будет добавлен в базу и whitelist без заявки.

---

## !EC удалить

Можно удалить:

### Ответом на сообщение:
```
!ЕС удалить
```

### Или по нику:
```
!ЕС удалить
Errnick_
```

Удаляет игрока:
- из базы бота
- из whitelist сервера

---

# 🔧 Смена префиксов

```js
const PREFIXES = ['!EC', '!ЕС'];
```

Можно изменить или добавить свои.

---

# 📦 Сборка

Команда:

```
!ЕС сборка
```

Отправляет `.mrpack` файл.

Поддерживается:
- Modrinth
- Prism Launcher
- другие лаунчеры с поддержкой mrpack

---

# 🖥 Требования

- Node.js 18+
- Включённый RCON
- Telegram Bot Token
- npm install node-telegram-bot-api rcon-client
- (Без библиотек работать не будет)

---

# 🔐 Безопасность

Не публикуйте:
- токены
- пароль RCON

---

# 📌 Примечание

Бот не ограничен только Minecraft.  
Если игра поддерживает RCON — бот будет работать.

---

Автор проекта: Errnick_
