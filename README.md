# EasyTGWhiteListMC

Telegram-бот для управления whitelist Minecraft сервера через RCON.

Подходит для:
- Локального запуска
- Запуска на хостинге
- Белый IP не требуется

Бот работает с любым сервером, где есть поддержка RCON.

---

## ⚙️ ОБЯЗАТЕЛЬНАЯ НАСТРОЙКА

### 1️⃣ Указать главного администратора

Найдите в коде:

```js
const ADMIN_FILE = path.join(BOT_FOLDER, 'admins.json');
const MAIN_ADMIN = 'Errnick'; // <-- заменить на свой username
let admins = [];
```

Замените `'Errnick'` на свой Telegram username.

---

### 2️⃣ Указать токены ботов

Поддерживается основной и тестовый бот.

```js
// ------------------------
// Режим теста
// ------------------------
const TEST_MODE = 1; // 0 — основной бот, 1 — тестовый бот

const TOKENS = {
  main: 'YOUR_MAIN_BOT_TOKEN', // основной бот
  test: 'YOUR_TEST_BOT_TOKEN'  // тестовый бот
};
```

Вставьте свои токены.

---

### 3️⃣ Настроить RCON

```js
// =========================
// RCON SETTINGS
// =========================
const rconConfig = {
  host: 'your_rcon_host',
  port: 12345,
  password: 'YOUR_RCON_PASSWORD'
};
```

Укажите:
- IP сервера
- Порт RCON
- Пароль RCON

---

## 📩 Подача заявки

Игрок может написать для примера заявки:

```
Заявка
```

или использовать префикс для списка всех команд:

```
!EC
```

(работает и на русском `!ЕС`)

Бот выведет список доступных команд.

---

## 🔧 Смена префиксов

```js
const PREFIXES = ['!EC', '!ЕС'];
```

Можете изменить или добавить свои префиксы.

---

## 🎮 Поддержка лицензии и пиратки

Бот автоматически определяет тип игрока из заявки.

```js
async function addToWhitelist(playerNick, playerType) {
  const cmd = playerType.toLowerCase().includes('пират')
    ? `easywhitelist add ${playerNick}`  // команда для пиратки
    : `whitelist add ${playerNick}`;     // для лицензии
  return sendRconCommand(cmd);
}
```

---

## 📦 Отправка сборки

Команда:

```
!ЕС сборка
```

Отправляет сборку в формате `.mrpack`.

Подходит для:
- Modrinth
- Prism Launcher
- и других лаунчеров с поддержкой mrpack

---

## 🚀 Возможности

- Автоподача заявок
- Разделение прав админов
- Работа через RCON
- Поддержка тестового режима
- Отправка сборки
- Поддержка любых игр с RCON

---

## 🖥 Требования

- Node.js
- Включённый RCON на сервере
- Telegram Bot Token

---

## 📌 Примечание

Бот не привязан только к Minecraft.  
Если игра поддерживает RCON — бот будет работать.

---

## 🔐 Безопасность

Не публикуйте:
- токены бота
- пароль RCON

Добавьте их в `.gitignore` при публикации проекта.

---

Автор проекта: Errnick_
